# Agent orchestration
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-anthropic>=0.2.0
# MCP tools at runtime (Polymarket docs MCP -> LangGraph tools)
langchain-mcp-adapters>=0.1.0

# Polymarket official SDK (Merakku v3.0 Layer 1 P0) — order book reads
py-clob-client>=0.17.0
# MarketLens (Merakku v3.0 Layer 1 P0) — tick-level L2 history / backtesting.
# SDK installs free; DATA ACCESS NEEDS A PAID MARKETLENS_API_KEY.
marketlens>=1.3.0
# RAG over markets (Polymarket/agents-style Chroma). Local embeddings, no key.
chromadb>=1.0.0

# HTTP / data
httpx>=0.27.0
python-dotenv>=1.0.0

# News (optional at runtime — collector degrades without a key)
tavily-python>=0.5.0

# Logging
loguru>=0.7.2

# Test
pytest>=8.0.0
